import {subtract} from './subtract'
describe('subtract', () => {
  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })

  it('should subtract two numbers', () => {
    expect(subtract(1, 2)).toBe(-1)
  })
})
